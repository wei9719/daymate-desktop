# DayMate 日伴使用手册

> 面向从 GitHub 下载和使用 DayMate 的普通 Windows 用户。无需安装 Node.js、Rust，也无需注册账号。

## 立即下载

[⬇️ **直接下载 DayMate 0.8.0 Windows 安装包（64 位）**](https://github.com/zhangweiguo9719-web/daymate-desktop/releases/download/v0.8.0/DayMate_0.8.0_x64-setup.exe)

- [查看最新版本与更新说明](https://github.com/zhangweiguo9719-web/daymate-desktop/releases/latest)
- [下载 SHA256 校验文件](https://github.com/zhangweiguo9719-web/daymate-desktop/releases/download/v0.8.0/SHA256SUMS.txt)
- 当前支持：Windows 10 / Windows 11，64 位
- GitHub 账号：不需要
- AI API Key：不需要；只有 AI 增强功能需要自行配置

以后发布新版本时，请优先进入“查看最新版本与更新说明”页面下载最新安装包。

## 1. 安装 DayMate

1. 点击上方“直接下载”，保存 `DayMate_0.8.0_x64-setup.exe`。
2. 双击安装包，按照安装向导完成安装。
3. 从桌面快捷方式或开始菜单打开“DayMate 日伴”。
4. 第一次启动时完成昵称、身份、陪伴语气、隐私权限和首个任务设置。

Windows 可能因为应用仍处于早期发布阶段而显示 SmartScreen 提示。如果安装包确实来自本仓库的 GitHub Releases，可选择“更多信息”后检查发布者和文件名，再决定是否运行。建议同时使用 Releases 中的 `SHA256SUMS.txt` 校验文件完整性。

### 校验安装包（可选）

在安装包所在文件夹打开 PowerShell，执行：

```powershell
Get-FileHash .\DayMate_0.8.0_x64-setup.exe -Algorithm SHA256
```

将输出结果与 `SHA256SUMS.txt` 中对应文件的值比较；完全一致再安装。

## 2. 第一次使用

首次引导会询问以下内容：

1. **昵称与身份**：用于调整首页称呼和内容风格，仅保存在本机。
2. **陪伴语气**：可选择更温柔、幽默、直接或鼓励的表达。
3. **隐私权限**：决定是否记录应用名称、窗口标题和电脑空闲状态。
4. **首个目标**：填写今天最想推进的一件事，也可以稍后再添加。

建议保留“应用活动记录”和“空闲检测”，并让“窗口标题记录”保持关闭。这样既能得到时间回顾，也能减少敏感信息进入本地记录。

## 3. 认识主界面

![DayMate 今日主界面](docs/screenshots/daymate-dashboard-v0.2.0.png)

上图为早期版本界面参考；下面的操作说明对应 0.8.0。

左侧导航用于切换主要功能：

- **今日**：问候、真实昨日简报、今日三件事、任务建议和精神补给。
- **时间回顾**：按本地日期查看应用使用时长、键鼠次数、排行与占比图。
- **任务**：添加、完成、删除任务，并设置优先级、预计时间和截止日期。
- **每日内容**：单独切换好句、音乐、轻松内容和微挑战。
- **数据与隐私**：暂停记录、调整隐私开关或删除活动数据。
- **设置**：主题、浮动球、音乐偏好和 AI 服务配置。

应用按日期选择柔和背景，也可以在设置中手动换景。同一天的默认选择保持一致。

首页“昨日简报”会读取前一个本地日期的真实活动记录，显示活跃时间、离开电脑时间和主要应用；没有记录才显示空态。读取失败会明确提示，不会把失败当成“昨天没有做事”。

## 4. 今日任务与专注

### 添加任务

进入“今日”或“任务”，填写任务名称，并按需要设置：

- 优先级
- 预计完成时间
- 截止日期

首页最多突出显示三项未完成任务。“帮我选一件”即使没有启用 AI 也能使用，它会优先考虑截止日期、优先级和预计时间较短的任务。

### 开始专注

1. 添加任务时选择预计时间，例如 5、15 或 25 分钟。
2. 在任务卡片上点击“开始”，按该任务的预计时间倒计时。
3. 点击“暂停”冻结剩余时间，准备好后点击“继续”。
4. 右上角关闭按钮的含义是“收起专注，保留计时”，不是放弃。主界面会出现“返回正在进行的专注”或“返回已暂停的专注”入口。
5. 想取消时点击“结束本次专注”并确认；不会因此把任务标记完成。

0.8.0 会把当前专注会话保存在本机。收起界面、切换页面、刷新或正常重开应用后可以恢复；同一任务重复点击“开始”不会重置尚未结束的会话。若改为另一项任务，会先询问是否结束当前专注。

计时按保存的**截止时刻**计算，不是简单数界面刷新次数。因此运行中的专注会把电脑休眠、应用退出期间经过的时间也算进去；如果不希望计时，离开前请先点“暂停”。回来时已超过截止时刻，会显示该段专注结束。

倒计时结束只代表这一段时间到了，**不会自动完成任务**。只有点击“完成任务”才会标记任务完成。当前版本恢复的是一段专注会话，并没有专注历史报表或累计“实际专注工时”；应用活动统计也不等同于专注计时。

如果暂时没有状态，可以先用 5 分钟启动。DayMate 的目标是帮你更容易开始，不会锁定电脑或强制关闭其他软件。

## 5. 音乐推荐与播放

DayMate 使用可解释的本地规则，根据时间、音乐类别、自选场景、心情及本机反馈整理候选；没有 AI Key 也可以使用。在线与本地歌曲共用一个播放器，切换 DayMate 页面或收起主窗口不会卸载播放器。

### 按心情挑选，不打断正在听的歌

1. 进入“每日内容”，选择“当前场景”和“此刻心情”。这些是你主动选择的状态，应用不会自动判断你的心理状况。
2. 选择“音乐陪伴方式”：**陪伴此刻**侧重与当前状态相配，**提一点精神**倾向更轻快的候选。休息、睡眠等场景仍优先安静风格。
3. 点击“按心情推荐”，生成最多 5 首候选；网络不可用时会注明离线曲库。
4. 展开候选下方的“为什么推荐”，查看本机排序依据；点击候选旁的播放按钮，才选中并播放它。

仅修改场景、心情或陪伴方式不会把正在听的歌换掉；点击推荐也主要更新候选列表，不抢走已有歌曲。你主动搜索、点选或导入的音乐，不会被迟到的旧推荐顶替。点击“换一首”、主动选曲或已开启的自动连播仍会按你的操作切歌。

### 喜欢、不喜欢与清除反馈

- 点击爱心表示“喜欢”，用于后续候选排序；再次点击可撤销。
- 点击“不喜欢”后，该曲不会进入后续候选；正在播放的歌不会被强行停止，可自行换曲。
- 展开“本机音乐反馈”，可以逐条撤销，或点击“清除本机音乐反馈”→“确认清除反馈”。不想清除时点“取消清除”。
- 最多保留 200 条反馈，只在本机使用，不发送给 AI。清除反馈不删除歌曲，也不清理任务或活动记录。

### 搜索与分类

- 可选择智能、专注、国风民乐、古典、自然或电子等类别。
- 搜索框支持歌曲名、歌手名或组合搜索，例如“张杰 天下”。
- 在线搜索使用 Audius 开放音乐目录，能否找到某首商业歌曲取决于该曲库是否收录并允许播放。
- 搜不到时可换用歌手名、英文名或关键词，也可以导入自己电脑中的本地音频。
- 本地导入支持 MP3、WAV、OGG、Opus、FLAC，单首不超过 100 MiB。应用会检查文件头并拒绝伪装网页/图片；提示格式或解码错误时请选择完整音频，不要只修改扩展名。导入失败会保留原歌曲。
- 导入后使用同一个上方播放器控制播放、暂停和进度；“清空本地歌曲”才会清空当前导入。音频仅在本次运行中使用，完全退出再启动需要重新导入，不会上传到曲库或 AI。
- 网络曲库不可用时，应用会回退到随安装包提供的 CC0 离线音乐，不影响任务等其他功能。

### 播放按钮

- **自动连播**：当前歌曲结束后继续播放下一首推荐。
- **随机推荐**：随机选择下一首歌曲。
- **顺序播放**：未选中随机或单曲循环时，按候选顺序选择下一首。
- **单曲循环**：当前歌曲结束后重新播放同一首。
- **换一首**：主动切换下一首歌曲。

“自动连播”关闭时，歌曲结束后不会自动切歌；单曲循环模式仍用于重复当前歌曲。推荐理由和音乐来源按钮不代表你必须跳转，正常听歌就在应用内完成。

音乐类别只影响音乐推荐，不会联动更换每日好句、轻松内容或微挑战。

## 6. 浮动球与后台运行

![DayMate 桌面浮动球](docs/screenshots/daymate-floating-ball.png)

- 点击侧边栏的“收起为浮球”，主窗口会隐藏，桌面上保留浮动球。
- 按住浮动球上方的拖动柄，可把它放到顺手的位置。
- 左键单击浮动球，恢复主窗口。
- 音乐播放时，浮动球显示律动柱。
- 右键单击浮动球，可显示主窗口、隐藏浮球或完全退出 DayMate。

关闭主窗口后，DayMate 可能仍在系统托盘中运行，以继续记录时间和播放音乐。这是正常行为。

## 7. 系统托盘与退出软件

系统托盘位于 Windows 任务栏右下角。DayMate 图标可能收在“显示隐藏的图标”箭头中。

- **左键单击托盘图标**：打开 DayMate 主窗口。
- **右键单击托盘图标**：打开原生菜单。
- 在右键菜单中选择“退出 DayMate”：先保存最后一段已采样活动记录，保存成功后再结束主窗口、浮动球、音乐和后台记录。

鼠标只是移到托盘图标上不会弹出主窗口。若要完全退出，请使用浮动球或托盘的右键菜单，不需要任务管理器。

如果保存失败，或 6 秒内没有收到保存确认，DayMate 会保留运行并打开主界面显示提示，不会悄悄退出。请检查数据盘剩余空间、目录权限或数据库是否被其他工具占用，然后再次从托盘选择“退出 DayMate”。强制结束进程、断电不具备这一保护；请不要在提示保存失败时直接强杀进程。

## 8. 时间回顾与键鼠活跃判断

![DayMate 时间回顾](docs/screenshots/daymate-review-v0.3.0.png)

上图为早期版本截图。0.8.0 可用右上角日期框选择已有记录的某一天，日期按电脑本地时区计算；图表、应用排行和键鼠次数会一起查询所选日期。没有记录和读取失败会分别提示，不会把今天的数据冒充成历史数据。

时间回顾会显示：

- 电脑活跃时长
- 每个应用的真实程序图标、停留时长和占比
- 应用时长排行图与环形占比分布图
- 完整应用明细和时间进度条
- 鼠标点击次数
- 键盘按键次数
- 最近一次键鼠输入时间
- 空闲时间
- 当天观察到的应用切换次数

“当前应用”和“最近一次键鼠输入”只在查看今天时显示。应用明细最多展示所选日期的前 100 项，排行图突出前 8 项；原始记录不会因此被删除。切换次数按当天相邻非空前台应用的变化计算：同一个应用的周期保存不计次数，空闲期间观察到的不同应用会计入，但仅从活跃变为空闲不算切换。

历史记录在升级后可能暂时使用首字母占位图标；再次使用对应应用并完成一次记录后，DayMate 会从本机程序文件自动补上真实图标。

DayMate 使用 Windows 的“最近一次键鼠输入时间”判断电脑是否真正处于使用状态。连续 5 分钟没有输入后，亮屏时间不会继续计入活跃时长，从而减少电脑长期开着但人已离开的误判。

这是约 3 秒采样的近似统计，不是精确工时计费。应用时长可能等待下一次批量保存才更新；退出前会刷新已聚合的部分，但不保证强杀、断电时尚在内存中的记录。你可在“数据与隐私”关闭空闲检测；关闭后不要再把活跃时长理解为已确认有人操作的时间。

为保护隐私，键鼠统计只保存次数，不保存：

- 具体按键内容
- 密码或聊天内容
- 鼠标坐标
- 点击目标
- 屏幕截图

## 9. AI 服务配置（可选）

没有 AI Key 时，任务、统计、每日内容、音乐搜索、背景和本地规则推荐仍可正常使用。

如需 AI 增强：

1. 打开“设置”中的“AI 服务”。
2. 开启“AI 增强”。
3. 选择服务商，例如商汤日日新、OpenAI、DeepSeek、通义千问、硅基流动、智谱、Moonshot、OpenRouter、Ollama 或自定义 OpenAI 兼容接口。
4. 核对 Base URL，填入 Key 并点击“保存密钥”；然后点击“获取可用模型”，筛选并选择文本模型，也可手动填写。
5. 点击“测试连接”，有效正文返回后才算成功。各服务商的地址和模型分别保留，切换回来无需重填普通配置。
6. 设置“每日 AI 调用上限”（默认 20 次）。需要按今日活动调整推荐时，再开启汇总分享。

API Key 保存在 Windows 凭据管理器，不会写入 Git 仓库、普通配置、SQLite 活动数据库或日志。不要把自己的 Key 发到 GitHub Issue、截图或公开聊天中。

点击“AI 按今日节奏推荐”会先展示发送预览，确认后才调用。默认发送音乐类别、自选场景、自选心情、当前小时和“陪伴此刻／提一点精神”；开启汇总分享后才发送活跃分钟数和未完成任务数量。不发送窗口标题、任务标题、正文、聊天内容或喜欢／不喜欢的记录。AI 只给出本次检索方向，本地规则再筛选候选，不保证曲库存在某首歌；不覆盖长期音乐类别偏好，也不联动每日好句。

独立鼓励仅发送自选场景、心情、当前小时和陪伴语气；不会附带活动摘要。失败时使用本地鼓励并标明来源。服务商、模型与权限的区别、费用说明及详细排错见[自带 API Key 指南](docs/ai-setup.md)。

相同条件的音乐和鼓励结果可在 15 分钟内复用，模型目录缓存 10 分钟。无网络、额度不足或返回格式无效时自动使用本地推荐。每日请求次数包含获取目录、测试连接、失败及重试，不等同于服务商账单；关闭再打开应用不会重置当天次数。测试连接会发送固定短句，可能按所选模型定价收费。

如服务地址不正确、密钥无权限或模型不可用，界面会给出具体处理提示。远程接口使用 HTTPS，本机 Ollama 可使用 HTTP；Base URL 不要附带 `/chat/completions` 或密钥参数。“删除密钥”只删除当前服务商的本机凭据，不会注销服务商账号。

从 0.6.1 起，密钥绑定保存时的接口地址。修改地址后看到“当前接口未获授权”是保护措施，请核实地址可信，再重新输入并保存密钥。旧版官方默认地址配置可继续使用；旧自定义地址需重新保存。若降级到旧版应用，新格式凭据需要重新配置，不影响活动数据库和任务。

如果本地设置损坏，应用会在覆盖前保留一份本机恢复副本；遇到“备份失败/已有不同恢复副本”时，先保留原 WebView 数据，不要反复重置或公开上传。恢复键为 `daymate-state-v1.recovery`，可请维护者协助在本机恢复。详细边界见[安全与兼容性说明](docs/security-compatibility.md)。

### 启动与通知

“设置 → 启动与通知”的开机启动开关读取 Windows 实际状态，开启后在登录时运行，关闭即撤销。自启保留当前活动数据目录；已完成引导时按浮动球偏好安静启动。

允许桌面通知后，专注倒计时结束或手动完成时可收到固定提示。可用“发送测试通知”检查；系统勿扰、通知禁用或开发版未注册都可能影响显示。界面会显示系统状态，提交成功表示已交给 Windows，并不保证横幅一定出现。

## 10. 数据与隐私

DayMate 默认采用本地优先设计：

- 活动记录保存在本机 SQLite 数据库。
- 任务、偏好、当前专注会话和音乐反馈保存在本机 WebView 数据中。
- 不要求账号，不提供活动数据云同步。
- 窗口标题记录默认关闭。
- 不记录键盘内容、不截屏、不读取聊天或文档正文。

正式安装版数据库通常位于当前 Windows 用户的应用数据目录：

```text
%APPDATA%\com.daymate.desktop\daymate.sqlite3
```

实际路径可能随 Tauri 或 Windows 环境变化，以“数据与隐私”页面显示的位置为准。清理活动数据时优先使用该页面的删除按钮；若要手动备份或移动数据库，先确认 DayMate 已通过托盘成功退出。清空活动记录不会删除任务、偏好、专注会话或音乐反馈。

开发仓库自带的 D 盘启动方式会把开发数据库保存到：

```text
D:\DayMate\data
```

完整隐私说明请阅读 [PRIVACY.md](PRIVACY.md)。

## 11. 更新软件

1. 打开 [GitHub 最新版本页面](https://github.com/zhangweiguo9719-web/daymate-desktop/releases/latest)。
2. 阅读版本更新说明。
3. 下载最新的 `DayMate_x.y.z_x64-setup.exe`。
4. 完全退出正在运行的 DayMate。
5. 运行新安装包完成覆盖安装。

更新前通常不需要卸载旧版本。涉及数据迁移时，请以对应 Release 的说明为准。所有正式更新纪要也可在 [CHANGELOG.md](CHANGELOG.md) 查看。

## 12. 卸载软件

1. 先通过托盘或浮动球右键菜单完全退出 DayMate。
2. 打开 Windows“设置 → 应用 → 已安装的应用”。
3. 找到 DayMate，选择“卸载”。

如果希望连同本地活动历史一起删除，请先在 DayMate 的“数据与隐私”页面清空数据。卸载前也可以自行备份数据库。

## 13. 常见问题

### 安装后双击没有反应

先检查系统托盘，应用可能已经在后台运行。仍无法打开时，重启 Windows 后再试，并在 GitHub Issue 中提供 Windows 版本、DayMate 版本和可复现步骤。

### 音乐提示“暂时走丢了”

这通常表示在线曲库临时不可用或当前搜索无结果。可以稍后重试、换关键词、切换类别或导入本地歌曲；任务和时间记录不会受影响。

### 搜不到想听的商业歌曲

DayMate 当前使用开放音乐目录，不绕过版权、会员或平台登录限制，因此无法保证收录所有商业歌曲。可尝试歌手名加歌曲名，或使用自己合法持有的本地音频。

### 切换窗口后音乐停止

0.8.0 将页面共用的音频和播放事件统一到独立播放器，包含本地导入。请确认没有在托盘菜单中完全退出 DayMate；当前歌曲可能结束、解码失败或在线源断流，界面会显示对应状态。若仍能复现，请提交 Issue。

### 收起专注后还能继续吗

可以。点主界面的“返回正在进行的专注”或“返回已暂停的专注”。运行中的会话按截止时刻计算，收起、退出或休眠不等于暂停；需要冻结剩余时间请先点“暂停”。

### 选择退出后软件为什么还在

先查看主界面是否提示活动记录保存失败或仍在等待保存。检查数据盘空间、目录权限和文件占用，解决后再次选择托盘“退出 DayMate”。这样可以保护最后一段记录，而不是直接丢掉它。

### 为什么关闭窗口后进程还在

DayMate 会最小化到托盘，以继续活动统计、音乐和浮动球。需要结束进程时，右键托盘或浮动球并选择“退出 DayMate”。

### 时间统计为什么比亮屏时间短

连续 5 分钟没有键鼠输入后会被判定为空闲，避免把人已离开但屏幕未熄灭的时间算作活跃使用。

### 如何报告问题

前往 [GitHub Issues](https://github.com/zhangweiguo9719-web/daymate-desktop/issues) 提交问题，并附上：

- Windows 版本
- DayMate 版本
- 问题现象与复现步骤
- 预期结果和实际结果
- 必要的界面截图

提交前请遮挡 API Key、窗口标题、任务内容、用户名和个人路径等隐私信息。

## 14. 给开发者

如果你希望从源码运行、参与开发或自行构建，请阅读：

- [README：技术栈、架构与构建命令](README.md)
- [开发环境说明](docs/development.md)
- [架构说明](docs/architecture.md)
- [工程手册：模块、状态、存储与排错](docs/engineering-handbook.md)
- [0.8.0 调研与技术取舍](docs/research-and-decisions-v0.8.0.md)
- [版本发布流程](docs/release.md)
- [更新日志](CHANGELOG.md)
- [贡献指南](CONTRIBUTING.md)

普通用户只需下载安装包，不需要克隆仓库或配置开发环境。

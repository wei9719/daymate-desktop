# Changelog

格式参考 Keep a Changelog，版本遵守 Semantic Versioning。

## [Unreleased]

### Added

### Changed

### Fixed

### Removed

### Security

## [0.6.1] - 2026-09-08

### Security

- AI 密钥绑定保存时的服务商及完整接口地址；改变地址后必须重新确认并保存，防止旧密钥误发。继续兼容旧版官方地址凭据，自定义旧凭据需重新保存。
- 主窗口和浮动球采用独立权限清单，浮动球不能调用活动数据、AI 凭据和系统设置命令；外部链接仅允许应用使用的来源站点。
- 校验音乐响应类型、长度和曲目 ID，不再接受返回值中的任意流媒体地址；固定从 Audius 官方入口播放，保留离线回退。
- 本地歌曲导入限制音频扩展名、MIME、文件头及 100 MiB 大小上限，使用固定音频类型的 Blob；拒绝网页、SVG 和伪装文件，错误提示不会清掉正在使用的歌曲。
- CodeQL 扫描报告加入高危阻断规则，报告缺失、格式异常或高危问题都会停止发布；Tag 同样执行扫描，不依赖人工查看告警。
- 更新存在已知问题的间接依赖；发布加入完整 npm 与 RustSec 审计，并固定第三方 GitHub Actions 的提交版本。全部 Rust 漏洞及 Windows 实际依赖中的 unsound 警示阻止发布，其他维护状态警示完整披露。

### Fixed

- 修复 Windows 长时间运行后输入时间戳回绕、小幅调时、休眠及跨午夜采样的统计边界。
- 暂停记录时停止按键状态扫描；清空记录时同步丢弃待写入数据，关闭标题记录后不再把内存中的旧标题写入数据库。
- 缩小统计读取的锁定范围，提取应用图标不再阻塞活动采样和记录开关；限制图标路径与缓存规模。
- 数据库升级改为事务处理，拒绝写入更新版本数据库，修复部分完成的旧迁移并保留有效历史。
- 校验旧任务和设置，防止异常存储覆盖应用方法；损坏数据写回前保留恢复副本，兼容不支持 randomUUID 的 WebView 环境。

### Changed

- 云端质量检查覆盖 Windows Server 2022/2025；同一个安装包需通过两种环境下的全新安装和 v4 数据升级测试，才允许公开发布。
- 安装测试覆盖中文及空格路径、历史数据保留与重复启动单实例；只有最终发布任务拥有 Release 写权限。

数据库迁移：结构仍为 v5，没有新增表；加固迁移事务与版本检查，保留现有活动记录。新格式 AI 凭据如需降级旧版应用，必须重新配置密钥。

## [0.6.0] - 2026-09-08

### Added

- AI 推荐前展示本次发送的字段；活跃分钟数与未完成任务数量改为单独选择分享，默认关闭。
- AI 每日请求上限默认 20 次，可设为 1–100 次，设置页显示本机当日请求次数。
- 相同条件下的 AI 推荐缓存 15 分钟，并区分 AI、近期缓存与本地推荐来源。
- 开机启动连接系统设置，支持读取真实状态、撤销和保留自定义数据目录；提供桌面测试通知。
- 发布前新增云端 Windows 安装与启动冒烟测试，检查安装、独立数据库初始化、持续运行和重复启动单实例行为。

### Changed

- AI 调用移至后台工作线程，等待模型时主窗口和悬浮球保持可操作。
- 发布说明自动来自本文件的对应版本，安装包及 SHA256 校验文件齐全后公开 Release。
- 版本校验覆盖锁文件、Git Tag 和界面版本；发布复用 CI 质量检查。

### Fixed

- AI 请求失败时自动选择本地音乐类别，连接测试验证真实的对话返回值。
- 修复异常模型 JSON 括号顺序可能导致崩溃；限制返回大小、类别和理由长度。
- 修复服务商切换时旧请求更新密钥状态、设置未保存和重复点击造成的混乱。
- 修复 Windows 构建脚本未将实际失败退出码传递给调用方。
- 修复浏览器预览直接调用桌面窗口接口导致的空白页。
- 统一 Git 检出与格式工具的 LF 换行，修复 Windows 云端构建的格式检查失败。

### Security

- 服务地址使用标准 URL 解析，仅允许真实本机地址使用 HTTP，并禁用 AI HTTP 重定向。
- 服务错误采用可操作的中文说明，不显示服务器原始响应、密钥或完整请求。
- 开启 WebView 内容安全策略，脚本限本地资源；AI 网络请求保留在 Rust 层。
- 限制同时只执行一个 AI 请求；短暂服务故障最多重试一次，响应超时不自动重复发送。

数据库迁移：升级到 v5，新增只保存日期与请求次数的 `ai_daily_usage` 表，保留原活动记录、任务与设置。

## [0.5.0] - 2026-09-08

### Added

- AI 服务新增硅基流动预设，默认使用经过实测的通义千问 `Qwen/Qwen2.5-7B-Instruct`。

### Changed

- AI 请求遇到连接失败或超时时会短暂等待并自动重试一次，降低临时网络握手波动造成的失败。

## [0.4.0] - 2026-07-16

### Added

- 时间回顾新增全部应用的详细列表，显示真实程序图标、应用名称、停留时长、时间占比和进度条。
- 新增应用时长横向排行图和环形占比分布图，并提供停留最久应用的中性摘要。
- Windows 程序图标从本机可执行文件提取，并在运行期间使用内存缓存。

### Changed

- 应用排行不再只显示前五名，时间回顾会列出当天所有产生有效活跃时长的应用。
- 数据库迁移至 v4，为活动会话保存可执行文件路径，以便后续稳定恢复应用图标。

### Security

- 可执行文件路径只保存在本机 SQLite，仅用于提取程序图标；图标和路径不会上传。

## [0.3.2] - 2026-07-15

### Added

- 新增面向 GitHub 普通用户的中文使用手册，包含安装包直链、安装、核心功能、浮动球、音乐、隐私、更新和故障排查说明。

### Changed

- 键鼠计数改为检测 Windows 实时按下状态的边沿，只在“未按下 → 按下”时累计一次。
- 不再把通用 Shift、Ctrl、Alt 与左右修饰键重复计数。

### Fixed

- 修复用户没有敲击键盘时，键盘次数仍持续自动增加的问题。
- 修复 `VK_CANCEL` 被错误归类为鼠标点击的问题。

### Security

- 数据库迁移至 v3，自动清除旧版本产生的不可靠键盘次数；应用时长、任务和鼠标历史保持不变。

## [0.3.1] - 2026-07-15

### Fixed

- DayMate 现在只允许一个运行实例；重复双击桌面图标只会唤醒已经运行的主窗口，不再创建新进程、新窗口或额外浮动球。
- 从桌面图标、托盘重新打开时会隐藏浮动球，并恢复、置前现有主窗口。
- 浮动球打开操作增加 500 毫秒防重复触发，避免电脑卡顿时连续点击产生重复请求。

## [0.3.0] - 2026-07-15

### Added

- 音乐播放器新增自动连播、随机推荐和单曲循环控制。
- 音乐播放时，桌面浮动球显示四柱律动状态。
- 时间回顾新增鼠标点击次数、键盘按键次数和最近一次键鼠输入时间。
- 托盘和浮动球新增原生右键菜单，可显示主窗口、隐藏浮球或完全退出 DayMate。

### Changed

- 电脑活跃时间使用 Windows 最近键鼠输入判断，连续 5 分钟无输入后不再把亮屏时间计为活跃。
- 活动数据库迁移至 v2，为每段会话保存键鼠次数汇总。

### Fixed

- 修复鼠标移动到托盘图标上时主窗口自动弹出的问题；现在只有托盘左键单击才打开窗口。
- 修复应用只能通过任务管理器退出的问题。

### Security

- 键鼠统计只保存次数，不记录按键内容、鼠标位置或输入目标。

## [0.2.0] - 2026-07-14

### Added

- 新增应用内智能音乐播放器，根据时间和待办状态从 Audius 大型开放音乐目录推荐并播放音乐。
- 新增四首 CC0 离线音乐、六类音乐偏好、开放曲库搜索和本地歌曲导入；断网时仍可播放。
- 新增 AI 音乐类别推荐，只发送活跃分钟、未完成任务数和音乐偏好等最小化汇总。
- 新增七套每日舒适背景，同一天保持一致，并支持用户手动切换。
- 新增多 AI 服务商设置：商汤日日新、OpenAI、DeepSeek、通义千问、智谱、Moonshot、OpenRouter、Ollama 与自定义 OpenAI 兼容接口。
- 新增 AI 接口连接测试和按服务商独立保存密钥。

### Changed

- 每日音乐由跳转外部平台改为在 DayMate 内直接播放。

### Fixed

- 修复音乐搜索偶尔只返回图片、导致“音乐暂时走丢了”的问题。
- 修复切换音乐类别时每日好句也被意外更换的问题。
- 修复切换 DayMate 页面或收起主窗口后音乐停止的问题，播放内核现在常驻应用生命周期。

### Security

- API Key 使用 Windows 凭据管理器保存，不写入 Zustand、SQLite、日志、Git 仓库或安装包。
- 远程自定义 AI 接口强制使用 HTTPS，仅允许本机 Ollama 使用 HTTP。

## [0.1.1] - 2026-07-14

### Added

- 新增可拖动的桌面浮动球，点击即可恢复 DayMate 主窗口。
- 设置页新增桌面浮动球开关，侧边栏新增“收起为浮球”快捷操作。
- README 新增真实应用界面截图。
- 新增 DayMate 正式品牌图标，并用于桌面、安装包和托盘。

### Changed

- GitHub Release 改为版本标签推送后自动公开发布 Windows 安装包。

## [0.1.0] - 2026-07-13

### Added

- 首次启动引导、今日首页、任务管理和专注倒计时。
- 基于截止日期、优先级和预计时间的本地任务推荐。
- 每日好句、音乐、轻松内容和微挑战。
- Windows 前台应用与空闲时间采集，SQLite 本地会话存储。
- 活动记录、窗口标题和空闲检测隐私开关。
- 时间回顾、系统托盘、深浅主题和本地偏好保存。
- 前端测试、版本一致性检查、GitHub CI 与 Release 工作流。

### Security

- 窗口标题默认关闭。
- 不采集键盘输入、屏幕截图、聊天正文或文档正文。
- 活动记录默认不上传。

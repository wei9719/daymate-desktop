# DayMate 隐私说明

## 记录什么

开启活动记录时，DayMate 记录前台应用进程名称、可执行文件路径、开始与结束时间、活跃与空闲秒数，以及鼠标点击和键盘按键的次数汇总。可执行文件路径只用于从本机程序文件提取应用图标，不读取该路径下的其他文件。只有用户主动开启“窗口标题记录”时才记录窗口标题。

DayMate 使用 Windows 提供的最近键鼠输入时间判断电脑是否真的在使用。连续 5 分钟没有输入后，保持亮屏的时间记为空闲，不计入活跃时间。

## 不记录什么

DayMate 不记录具体按键、输入内容、鼠标坐标或点击目标，不截屏，不读取聊天、网页或文档正文，不录制麦克风或摄像头。除提取程序图标、用户主动导入音频和本地 AI 显式选定的模型文件外，不扫描本地目录或读取其他应用数据。

## 数据在哪里

桌面活动和可执行文件路径保存在操作系统为 `com.daymate.desktop` 分配的应用数据目录中的 `daymate.sqlite3`，也可使用自定义数据目录。提取后的图标只在应用运行期间保存在内存缓存，不上传、不另建图标档案。数据与隐私页展示实际活动数据路径；它不代表 WebView 存储和系统凭据也在同一目录。

任务和偏好保存在 WebView 本地存储。当前专注会话另存于 `daymate-focus-v1`，包括任务引用/标题、截止时刻或暂停剩余时间和通知状态；它不上传 AI，也不是完整专注历史。

音乐喜欢/不喜欢另存于 `daymate-music-feedback-v1`，最多 200 条，包括曲目 ID、标题、音乐人、类别、评价和更新时间。仅用于本机排序，不发送给 AI 或音乐目录。当前场景、心情、音乐意图与最近 30 个播放 ID 只在内存中，重启不恢复；导入音频不自动复制、上传或公开分发。

## 是否联网

智能音乐会向 Audius 发送通用场景词与目标心情标签，或用户主动输入的歌曲搜索词，并从 Audius 官方播放入口加载音频。检索词和标签可能表达用户选择的音乐方向，因此不把它们笼统称为“毫无个人信息”。任务标题、活动明细、窗口标题、喜欢/不喜欢和近期播放 ID 不随请求发送；网络不可用时，非搜索推荐可使用安装包内的 CC0 曲目。关闭 AI 不等于关闭在线音乐。

AI 为可选功能。服务商、Base URL 和模型名称保存在本机普通设置中；API Key 按服务商分别保存在 Windows 凭据管理器，不写入 SQLite、WebView localStorage、日志、Git 仓库或安装包。新保存的凭据绑定服务商与完整接口地址（协议、主机、端口和路径），修改地址后必须重新输入并保存密钥。旧版凭据仅在对应官方默认地址继续可用，自定义接口需重新保存。点击“测试连接”时只发送固定测试短句，不发送任务或活动数据。

0.9.0 的 `local` 服务商不需要 API Key，也不会读取或向本机服务转发云端密钥。用户必须独立启动 `127.0.0.1:8765` 上的 Python 服务，并明确选定已有模型目录。服务通过 PyTorch/Transformers 离线读取 safetensors、模型配置和分词器，不下载模型，不读取其他项目的 RAG、聊天日志或 LoRA，不训练或修改权重。提示词和回答仅用于当前内存中的推理，不写入服务日志或活动数据库；启动状态与运行文件在所选运行目录中，与模型和 DayMate 数据目录分离。

本地模型请求不离开本机，但无 Key 的回环服务不是同机用户或进程之间的安全隔离。不要把它通过端口转发、代理或防火墙规则公开到局域网/互联网；不需要本地 AI 时可停止服务。本地 AI 不会关闭 Audius 音乐请求，也不会禁止用户显式选择云端服务。

“检查本地模型状态”只有点击后才访问健康接口，读取就绪状态、模型标识与设备信息，不发送聊天内容、不读取凭据、不计入 AI 请求预算，也不在后台轮询或启动模型。

AI 推荐先显示发送预览，由用户确认后请求。默认发送当前音乐类别、自选场景、自选心情、音乐意图（陪伴此刻或提一点精神）和本地当前小时。开启“分享活动汇总”后，才会额外发送今日活跃分钟数与未完成任务数量；不发送任务标题、窗口标题、文档内容、应用活动明细、音乐反馈或近期播放列表。关闭分享时，这两个汇总字段不进入模型消息。心情不由活动数据推断。

独立鼓励仅发送自选场景、心情、当前小时与陪伴语气，始终不附带活动摘要或音乐意图。获取模型目录只读取目录，云端需要鉴权、本地服务免 Key，不发送聊天消息；用户点击后才请求，不因输入密钥或切换服务商自动请求。模型地址和名称按服务商分别保存在本地偏好中，不包含密钥。0.9.0 不生成或上传图片，背景仍为本地渐变。

本机 `ai_daily_usage` 表只记录日期和请求次数，用于每日调用上限；包含模型目录、连接测试、失败请求及重试，不是服务商账单。相同条件的音乐和鼓励缓存 15 分钟，模型目录缓存 10 分钟；缓存只在内存中，修改密钥或退出应用后失效。服务返回错误不会连同原始响应显示或记录。

远程 AI 地址必须使用 HTTPS，只有真正的本机地址允许 HTTP。AI 请求不自动跟随重定向。界面启用内容安全策略，远程音乐只作为音频加载，远程脚本不能运行。

首次启动完成引导前不采集活动。开启系统自启时，注册的启动项包含程序路径和数据目录参数，保留用户选用的数据位置。通知只包含 DayMate 固定提示，不包含任务标题或活动内容。

## 删除与关闭

浮动球只被授权基础窗口操作、接收播放状态和显示右键菜单，不能通过原生命令读取活动数据库、操作密钥或修改自启。暂停活动记录后不再扫描按键状态；关闭窗口标题记录会清除尚未落库的标题，已保存的历史标题不会自动删除。

损坏的任务/偏好 JSON 在后续写入前会备份至 WebView 本地存储的 `daymate-state-v1.recovery`；备份只在本机，可能包含原任务与偏好，不上传。若已有不同恢复副本或存储空间不足，则保护原记录并拒绝覆盖。恢复副本也属于个人数据，不应粘贴到公开 Issue。

用户可以关闭活动采集、保持窗口标题关闭，并在“数据与隐私”页清空活动数据。清空操作需要二次确认。

音乐区可撤销单首评价，或二次确认后清除全部音乐反馈及内存近期列表。清空活动数据库不会自动删除任务、音乐反馈、当前专注会话或系统凭据。专注可主动结束；音乐反馈和专注存储也使用同键加 `.recovery` 的损坏数据保护，恢复副本可能包含个人内容，不能公开上传。

正常退出会先确认待保存活动已写入；失败时阻止本次退出并提示重试。任务管理器强制结束或断电可能丢失尚在内存中的采样，不承诺绝对无损。

“设置 → AI 服务”可关闭 AI、关闭汇总分享或删除所选服务商的本机密钥；删除密钥会清除内存推荐缓存。清空活动数据不重置每日 AI 调用次数。开机启动可在设置中随时关闭，通知开关只控制 DayMate 是否发送，Windows 自身仍可以阻止或静默展示通知。

关闭应用内 AI 或退出 DayMate 不会自动停止独立的本地模型进程。使用 `scripts/local-ai.ps1 -Action stop` 停止本轮脚本管理的服务；不会因此删除模型文件、任务或活动数据库。具体运行目录和停止方式见[本地 AI 指南](docs/local-ai.md)。

## 隐私问题

请通过 GitHub 的 `Privacy concern` Issue 模板报告。公开日志前请移除用户名、路径和窗口标题。
